# Data Guru

For Sheet sync without localhost, use the hosted GitHub Pages version:

`https://guru-sheru.github.io/data-guru/`

In Google Cloud Console, the OAuth client needs this authorised JavaScript origin:

`https://guru-sheru.github.io`

Opening `DataGuru.html` directly still works for filtering and downloading, but Google blocks Sheet sync from local `file://` pages.
